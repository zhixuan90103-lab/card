# 2026-07-22 · 路径锁 H1 落地

## 玩家向

- 每把锁对应的 **同点同色** 全场只留 **2～4 张**（含锁），钥匙不再遍地。  
- 困难局尽量只发「能清完」的牌；极难仍更狠，但钥匙同样稀缺。  

## 实现

- `pathLockMetrics.ts`：稀缺统计 + `enforceLockKeyScarcity`（改色压密度）  
- `level01Deal`：上色后 enforce；发局过滤密度 / 桌面钥匙率 / 贪心可清  

## 验证

- 单测：`H1: each lock match-key count is 2..4`  
- 检索计划 R5-H1 对应项  
