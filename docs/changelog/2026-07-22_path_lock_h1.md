# 2026-07-22 · 路径锁 H1 落地

## 玩家向

- 每把锁对应的 **同点同色** 全场只留 **2～4 张**（含锁），钥匙不再遍地。  
- 困难局尽量只发「能清完」的牌；极难仍更狠，但钥匙同样稀缺。  
- **库内钥匙靠前**（D25）：早抽到、可被盖、洗回；不钉最后一张。  
- **残局 trim**（D26）：桌上不需要的库牌成对回收。  

## 实现

- `pathLockMetrics.ts`：稀缺统计 + `enforceLockKeyScarcity`  
- `level01Deal`：enforce；`orderStockKeysFront`；发局过滤  
- `rules.trimSurplusDeck` + `state` 配对后调用  

## 验证

- 单测：H1 稀缺、库钥匙靠前、trim、清桌 reclaim  
- 总表：`2026-07-22_full_roundup.md`  
