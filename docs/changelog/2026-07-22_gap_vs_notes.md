# 2026-07-22 · 笔记 vs 实现：缺口对齐

**目的：** 对照 `08` / `13` / `11` / `15` / HANDOFF，诚实回答「还有哪些没做」。  
**结论一句话：** **M0 已完成且 Level01 规则定稿可玩；M1「多关内容 + 真机/外放」未完成；`13` 里部分勾选曾超前于代码，已按本文纠偏。**

---

## 1. 总览

| 档 | 笔记目标 | 实际状态 |
|----|----------|----------|
| **M0** 垂直切片 | 1 关完整规则 + phone-frame | **已完成**（且 Level01 远超「手摆 JSON」：固定几何 + seed 配点） |
| **M1** 可演示 | 10～15 关 + 教学 + 顿悟关 + 真机 | **未完成**（仅 1 个关卡模板 + 重开换 seed） |
| **Level01 规则债** | 平行剥 / 清桌胜利 / 精简 stock | **已完成**（见 `2026-07-22_level01_session_summary.md`） |

---

## 2. 已做（笔记应对齐为完成）

### 2.1 工程与 M0 规则

- Vite + TS + Pixi 8 + Vitest  
- phone-frame 393×852 letterbox  
- core：`isFree` / 配对 / 抽洗 / 撤销 / 重开  
- 胜利 = **清谜题区**（D10）；库回收（D10b）  
- 软卡提示 + 硬死局弹窗  
- 占位牌面 + 飞走动画 + DOM HUD  

### 2.2 Level01 现行（相对旧「手摆 1 关」是增强）

| 能力 | 状态 |
|------|------|
| 固定满槽几何 L0/L1/L2 | 有 |
| seed 随机点数 / 锁 / stock | 有 |
| 禁平行剥 R15 | 有 |
| 精简 stock R7 | 有 |
| L1 硬锁 + 钥匙拓扑 | 有（外观无特殊皮肤，符合 D13） |
| 重开 = 新 seed 同几何 | 有 |
| 单测 `level01` / cover / rules | 有 |

### 2.3 设备相关（半完成，代码有钩子）

| 项 | 代码 | 真机验收 |
|----|------|----------|
| `viewport-fit=cover` | 有（index.html） | 未勾 `11` §6 |
| `touch-action` | 有 | 未勾 |
| HUD `safe-area-inset` | 有（hud/css） | 未勾 |
| `visualViewport` resize | 有（app.ts） | 未勾 |
| WebGL context lost/restored | **仅 log**，未完整重建资源 | 未勾 |

---

## 3. 未做 / 笔记写了但代码没有

### 3.1 M1 内容（`13` Phase D · 原勾选有误）

| ID | 笔记目标 | 实际 | 建议 |
|----|----------|------|------|
| **D1** | 10～15 关手摆 JSON | **只有 `level-01` 一条**（`LEVELS.length === 1`） | 要么做多关，要么改笔记为「单关 + seed 变体」即 M1 内容策略 |
| **D2** | 关 1～2 独立教学关 | 仅 `teachHint` 一行文案 | 拆教学关或强化首局引导 |
| **D3** | ≥3 关顿悟候选 + 设计瓶颈注 | `insightNote` 现为 deal meta JSON，非「顿悟设计句」 | 做 3 个手调 seed/布局或改验收定义 |
| **D6** | 下一关 / 关卡选择 | `nextLevel` 恒空；无选关 UI | 多关后才有意义 |
| **D7** | 占位美术升级（可选） | 矩形 + 文字 | 可选 |

### 3.2 M1 门禁未勾

| 门禁 | 状态 |
|------|------|
| 外部玩家话术达标（`03`） | **未做** 2～3 人外放 |
| iPhone 15 清单（`11` §6） | **未做** 真机逐项勾选与留痕 |
| 10～15 关可进 | **未做**（现 1 关） |

### 3.3 Phase E 收口

| ID | 内容 | 状态 |
|----|------|------|
| E1～E2 | meta / safe-area | 代码基本有，**清单未验收** |
| E2b | visualViewport | 有监听，**真机未验** |
| E2c | context 完整恢复 | **未做**（只有 warn） |
| E3 | iPhone 15 §6 | **未做** |
| E4 | 翻面/飞走手感 | 有基础飞走，**未调优** |
| E5 | 外放 2～3 人 | **未做** |
| E6 | 文档与代码一致 | 本次 gap 文档在补 |

### 3.4 旧笔记仍写「不做」但实现已做（需改笔记口径）

| 旧表述（`08` / HANDOFF） | 实际 |
|---------------------------|------|
| 不做关卡生成器 | **已有** `level01Deal` seed 生成（几何固定） |
| 不做锁钥特殊规则 | **已有** L1 硬锁 + 钥匙（无特殊皮肤） |

→ 应改为：**允许 Level01 程序配点 + 无皮肤硬锁**；不做「千关 / 广告向生成器」。

### 3.5 配点理想项（`15` 目标，非阻塞）

| 项 | 状态 |
|----|------|
| 反向落子保证可解 | **未做**；`canFullyClear` 贪心过滤 + fallback |
| 多关差异化节奏曲线 | **未做**（单模板） |
| 美术牌面 / 音效 | **未做** |

---

## 4. 建议优先级（下一步）

### P0 · 先把笔记和现实对齐（文档 0.5h）

1. `13`：D1～D3/D6 改回 `[ ]` 或改为「Level01 seed 策略」新定义  
2. `08`：勾选已实现规则；「不做生成器」改为「不做千关生成器」  
3. 标题 `index.html` 仍写 M0 → 可改为 M0+ / Level01  

### P1 · 若目标仍是「可外放 M1」

1. **内容策略二选一**（需产品拍板）：  
   - **A.** 10～15 个**固定 seed** 关卡表（同几何不同点/锁）+ 教学文案  
   - **B.** 继续单关无限 seed，用「难度档 / 每日」代替多关  
2. 真机：按 `11` §6 勾清单并记阻塞  
3. 外放 2～3 人话术（`03`）  

### P2 · 体验打磨（非门禁）

- 消牌/翻面手感、简单牌背图  
- WebGL restore 完整重建  
- 可解性加强（反向 deal 或更强 solver）  

---

## 5. 与「关卡已正确」的关系

玩家已确认 **Level01 单关规则正确**（平行剥 / 清桌 / stock 精简）。  
这关闭的是 **规则债**，不关闭 **M1 内容量与真机外放**。

```text
规则正确 (Level01)  ✅
M0 可玩             ✅
M1 多关+外放        ❌  ← 当前主缺口
真机清单            ❌
```

---

## 6. 勾选纠偏记录（写入 `13`）

见同日对 `13_mvp_plan_and_todolist.md` 的状态修正。  
